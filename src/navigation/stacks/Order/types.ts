import { EScreens } from '../../screens.ts';

export type TOrderStack = {
    //          NAME                        PARAMS OR undefined
    // [EStacks.(some stack)]: TNavigationScreenParams<TNameStack>
    [EScreens.OrderMain]: undefined
}